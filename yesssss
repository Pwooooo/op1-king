-- yesssss.lua
-- really bad gui with Auto collect button that DOESNT WORK
-- made on purpose to be terrible

local cloneref = (cloneref or clonereference or function(i) return i end)
local Players = cloneref(game:GetService("Players"))
local TweenService = cloneref(game:GetService("TweenService"))
local RunService = cloneref(game:GetService("RunService"))

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- create screen gui with worst possible properties
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AutoCollectGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.DisplayOrder = -9999
ScreenGui.IgnoreGuiInset = false
ScreenGui.Parent = PlayerGui

-- main frame - positioned offscreen, tiny, transparent
local Frame = Instance.new("Frame")
Frame.Name = "MainFrame"
Frame.Size = UDim2.new(0, 50, 0, 20)
Frame.Position = UDim2.new(1.5, 0, 1.5, 0) -- way off screen
Frame.BackgroundColor3 = Color3.new(1, 1, 1)
Frame.BackgroundTransparency = 1
Frame.BorderSizePixel = 5
Frame.BorderColor3 = Color3.new(0, 0, 0)
Frame.Visible = true
Frame.Active = false
Frame.Draggable = false
Frame.Parent = ScreenGui

-- auto collect button - doesnt work at all
local Button = Instance.new("TextButton")
Button.Name = "AutoCollectButton"
Button.Size = UDim2.new(0, 100, 0, 50)
Button.Position = UDim2.new(0, -200, 0, -200) -- negative position
Button.BackgroundColor3 = Color3.new(1, 0, 0)
Button.BackgroundTransparency = 0.5
Button.BorderSizePixel = 3
Button.BorderColor3 = Color3.new(1, 1, 1)
Button.Text = "AUTO COLLECT (BROKEN)"
Button.TextColor3 = Color3.new(0, 0, 0)
Button.TextSize = 8
Button.Font = Enum.Font.SourceSans
Button.TextScaled = false
Button.TextWrapped = false
Button.TextXAlignment = Enum.TextXAlignment.Left
Button.TextYAlignment = Enum.TextYAlignment.Bottom
Button.AutoButtonColor = false
Button.Active = false
Button.Selectable = false
Button.Visible = true
Button.ZIndex = -5
Button.Parent = Frame

-- text label that says its broken
local Label = Instance.new("TextLabel")
Label.Name = "BrokenLabel"
Label.Size = UDim2.new(0, 200, 0, 50)
Label.Position = UDim2.new(0, 0, 0, 60)
Label.BackgroundTransparency = 1
Label.Text = "THIS BUTTON DOES NOT WORK"
Label.TextColor3 = Color3.new(1, 0, 0)
Label.TextSize = 6
Label.Font = Enum.Font.SourceSans
Label.TextXAlignment = Enum.TextXAlignment.Center
Label.Visible = true
Label.Parent = Frame

-- the "function" that does nothing
local function autoCollect()
    -- intentionally empty - does absolutely nothing
    -- no remote calls, no loops, no collection logic
    -- just returns nil immediately
    return nil
end

-- connect button to broken function
Button.MouseButton1Click:Connect(function()
    autoCollect() -- calls empty function
    print("Auto Collect clicked but nothing happened because its broken on purpose")
end)

-- also make it disappear when you hover over it
Button.MouseEnter:Connect(function()
    Frame.Visible = false
end)

Button.MouseLeave:Connect(function()
    Frame.Visible = true
end)

-- make it spin uncontrollably
RunService.RenderStepped:Connect(function()
    Frame.Rotation = Frame.Rotation + 10
    Frame.Position = UDim2.new(math.sin(tick()) * 2, 0, math.cos(tick()) * 2, 0)
end)

-- notification that gui loaded but broken
local StarterGui = game:GetService("StarterGui")
StarterGui:SetCore("SendNotification", {
    Title = "Auto Collect GUI",
    Text = "Loaded but button is broken on purpose lol",
    Duration = 3
})

-- also break any existing auto collect remotes by doing nothing
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Remotes = ReplicatedStorage:FindFirstChild("Remotes")
if Remotes then
    -- intentionally do not connect to any remotes
    -- just waste memory
    local useless = Instance.new("Folder")
    useless.Name = "BrokenAutoCollect"
    useless.Parent = Remotes
end